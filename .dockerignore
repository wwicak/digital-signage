# Git
.git
.gitignore

# Node / Bun modules
node_modules
*/node_modules

# Next.js build output (will be created inside the container)
.next
# Our server build output (will be created inside thecontainer)
dist

# Local environment variables file (should be handled by docker-compose or environment variables)
.env
*.env.local
.env.*.local

# Log files
*.log
logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
bun-debug.log* # If bun generates debug logs with this pattern

# Test files and directories (if not needed in the image for some reason, e.g. running tests in a separate step)
__tests__
coverage

# OS-specific files
.DS_Store
Thumbs.db

# Editor/IDE specific files
.vscode/
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Docker files themselves (no need to copy them into the image)
Dockerfile
Dockerfile.dev
docker-compose.yml

# Temporary files
*.tmp
*.temp
